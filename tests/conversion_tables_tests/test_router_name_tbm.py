from conversion_tables import RouterNameTbm

def test_content():
    '''
    This test doesn't need to run every time;
    this data is changeable, and therefore the 
    test can be invalid
    '''
    # net_id, router_id, asset_id, custom1 = RouterNameTbm().collect([46706936])

    # assert net_id == [46706936], "The net ID doesn't match"
    # assert router_id == [1553293], "The router ID doesn't match"
    # assert asset_id == ['DEMO'], "The asset ID doesn't match"
    # assert custom1 == ['RJH-Baseball'], "The Custom1 doesn't match"
    